# TODO

This file lists potential future enhancements for the modern `glm-js` implementation.

- **Code Formatting:** Integrate a code formatter like Prettier to ensure a consistent coding style across the project.
- **Linting:** Set up a linter like ESLint to automatically catch common errors and enforce best practices.
- **Automated CJS Build:** Create a build process (e.g., using Rollup or a similar tool) to automatically transpile the ES Module source into a CommonJS version for broader Node.js compatibility.
